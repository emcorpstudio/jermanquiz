<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="emcorp.studio.jermanquiz">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ActivitySplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".MenuBelajarActivity" />
        <activity android:name=".HomePercobaanActivity" />
        <activity android:name=".MendengarkanPercobaanActivity" />
        <activity android:name=".MenulisPercobaanActivity" />
        <activity android:name=".MelihatPercobaanActivity" />
        <activity android:name=".MencocokkanPercobaanActivity" />
        <activity android:name=".MenuTentangActivity"></activity>
    </application>

</manifest>